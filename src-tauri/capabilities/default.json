{
  "$schema": "https://schema.tauri.app/capability.json",
  "identifier": "default",
  "description": "Default capability for learnMe",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-save",
    "dialog:allow-open"
  ]
}
